<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Granted once via adb: pm grant brobata.physiboard android.permission.WRITE_SECURE_SETTINGS -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Embedded wireless-ADB pairing/connect (self-contained test surface) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Boot: post the backlight guidance notification (no service started) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <service
            android:name="it.palsoftware.pastiera.physi.KeyboardBacklightTileService"
            android:exported="true"
            android:icon="@drawable/ic_lightbulb_24"
            android:label="@string/backlight_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Vendored Shizuku ADB pairing foreground service -->
        <service
            android:name="moe.shizuku.manager.adb.AdbPairingService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- On boot, post a guidance notification if the backlight can't work yet. No service is
             started from boot (a foreground-service-start from BOOT_COMPLETED crashes on this OEM).
             Prompt re-arm now lives in the IME's KeyboardBacklightManager. -->
        <receiver
            android:name="it.palsoftware.pastiera.physi.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
